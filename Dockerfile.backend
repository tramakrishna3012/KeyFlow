# KeyFlow Application - Backend Service Container
FROM node:20-alpine

WORKDIR /app

# Set production environment
ENV NODE_ENV=production \
    PORT=4000

# Install production dependencies
COPY backend/package*.json ./
RUN npm ci --only=production --ignore-scripts && npm cache clean --force

# Copy application source files
COPY backend/src ./src

# Create non-root system user and adjust permissions
RUN addgroup -g 1001 -S keyflowgroup && \
    adduser -u 1001 -S keyflowuser -G keyflowgroup && \
    mkdir -p /app/data && \
    chown -R keyflowuser:keyflowgroup /app

USER keyflowuser

EXPOSE 4000

CMD ["node", "src/server.js"]
