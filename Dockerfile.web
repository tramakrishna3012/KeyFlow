# Look System Application - Web Dashboard Container
FROM nginx:alpine

# Copy custom Nginx configuration with SPA fallback and caching headers
COPY web/nginx.conf /etc/nginx/conf.d/default.conf

# Copy static website assets
COPY web/ /usr/share/nginx/html/

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
